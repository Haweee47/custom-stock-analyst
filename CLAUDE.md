# 프로젝트: 초소형주 맞춤형 AI 리포트 생성기

## 역할
초소형주 맞춤형 AI 리포트 생성기 개발을 돕는 시니어 AI 페어 프로그래머

## 개발 환경
VS Code, Python, GitHub, Notion

## 핵심 규칙
1. 코드는 [데이터 수집 → API 연동 → Gemini 분석 파이프라인 → 리포트 생성] 단계별로 제시할 것.
2. 특정 코드 작업이 완료될 때마다 깃허브 커밋 명령어(git add ., git commit -m "...", git push)를 항상 함께 제안할 것.
3. 세션 종료 또는 기능 완료 시 노션 학습 일지(목표, 핵심 코드 요약, 에러 해결 내역, 배운 점) 마크다운 템플릿을 자동으로 출력할 것.
4. 초보자 기준에서 가상환경 명령어와 라이브러리 사용 이유를 친절하고 명확하게 설명할 것.

## 프로젝트 현황 (2026-08-29 기준)

### 파이프라인 진행 상태
- [x] **1단계 데이터 수집** — `src/collectors/stock_collector.py`
- [ ] **2단계 API 연동** — `src/api/` (DART 재무·공시)
- [ ] **3단계 Gemini 분석** — `src/analysis/`
- [ ] **4단계 리포트 생성** — `src/report/`

### 기술 결정 사항
- **주가 데이터는 네이버 금융 스크래이핑을 쓴다. pykrx는 쓰지 않는다.**
  KRX가 정책을 바꿔 `data.krx.co.kr` API가 회원 로그인(`KRX_ID`/`KRX_PW`)을 요구하게 됐고,
  개인 로그인 정보를 자동화에 넣는 리스크를 피하기 위해 대체 소스로 전환했다.
- 네이버는 **시가총액을 억원 단위**로 제공하므로 원 단위 환산이 필요하다.
- 수집 목록에 ETF/ETN이 대량 포함되므로 `종목구분` 컬럼(보통주/우선주/ETF/ETN)으로 분류한다.
  재무제표가 없는 ETF·ETN은 Gemini 분석 대상에서 제외한다.
- **Notion Integration은 토큰만으로 동작하지 않는다.** 대상 페이지에서 Connections로
  Integration을 명시적으로 공유해야 API가 페이지에 접근할 수 있다.

### 필요한 API 키 (`.env`)
| 키 | 발급처 | 상태 |
|---|---|---|
| `NOTION_API_KEY` | notion.so/my-integrations | 설정 완료 |
| `NOTION_PARENT_PAGE_ID` | 학습 일지 부모 페이지 | 설정 완료 |
| `DART_API_KEY` | opendart.fss.or.kr | **미설정** |
| `GEMINI_API_KEY` | aistudio.google.com | **미설정** |

## 아키텍처 결정: 공개 Streamlit 웹앱 (2026-08-29 확정)

> 초안의 '전 종목 매일 배치 분석'은 비용 계산 결과 폐기했다. 아래가 확정안이다.

결과물은 **Streamlit 웹앱**이며 **인터넷에 공개 배포**한다. 이 결정이 파이프라인에 주는 제약:

### 1. 비용이 드는 것과 안 드는 것을 구분한다
- **무료**: 네이버 금융 스크래이핑, DART API(일 20,000건 한도). 매일 전 종목 배치로 돌려도 비용 0.
- **유료(후불)**: Gemini API. **여기서만 비용이 발생하므로 Gemini 호출만 아끼면 된다.**

### 2. Gemini는 온디맨드 + 캐시로 호출한다 (전 종목 배치 금지)
**실측 기준**(gemini-3.1-flash-lite, 입력 434 / 출력 310 토큰): 1종목 **0.80원**.

| 방식 | 비용 |
|---|---|
| 전 종목(2,655개) 1회 분석 | 2,132원 |
| 전 종목 매일 배치 | **월 63,951원** |
| 하루 20건 온디맨드 | **월 482원** |
| 일일 상한 100건을 매일 채워도 | 하루 80원 (월 2,400원) |

133배 차이다. 일일 상한 100건이 걸려 있으므로 최악의 경우에도 하루 80원을 넘지 않는다.

따라서:
- 방문자가 종목을 열면 캐시를 먼저 확인하고, 있으면 Gemini를 호출하지 않는다.
- 캐시에 없을 때만 생성하고, 결과를 `data/processed/`에 저장해 다음 방문자에게 재사용한다.
- **일일 신규 생성 상한**(예: 100건)을 둔다. 이것이 최악의 경우 비용 상한선 역할을 한다.
- 캐시는 새 분기보고서가 나오거나 7일이 지나면 만료시킨다.
- 시총 상위 소수(20~30개)만 미리 생성해 첫 화면이 비어 보이지 않게 한다.
- 모델은 **gemini-3.1-flash-lite**부터 시작한다(1종목 약 2.8원). 품질이 부족하면 상위 모델로 올린다.
- SDK는 **`google-genai`**를 쓴다. `google-generativeai`는 구버전이라 쓰지 않는다.
- `gemini-2.5-*` 계열은 신규 키로 호출되지 않는다(“no longer available to new users”).

### 3. 데이터 수집 배치와 분석은 분리한다
- **수집 배치(매일, 무료)**: 네이버 시세 + DART 재무 → `data/raw/`, `data/processed/`
- **분석(온디맨드, 유료)**: 방문자 요청 시 저장된 데이터로 Gemini 호출 → 캐시

### 2. API 키는 Streamlit Secrets로도 읽을 수 있어야 한다
로컬은 `.env`, 배포 환경은 Streamlit Cloud의 Secrets를 쓴다.
키를 읽는 코드는 두 경로를 모두 지원해야 한다 (`st.secrets` → 없으면 `os.getenv`).
**`.env`나 API 키가 저장소에 올라가면 안 된다.**

### 3. 투자 관련 법적 고지가 필요하다
불특정 다수에게 종목 분석을 제공하는 것은 자본시장법상 유사투자자문업 이슈가 있을 수 있다.
- 모든 페이지와 리포트에 **투자 판단의 책임은 이용자 본인에게 있다**는 면책 문구를 넣는다.
- 특정 종목의 매수/매도를 권유하는 표현을 쓰지 않는다. Gemini 프롬프트에도 이 제약을 명시한다.
- 공개 범위를 넓히기 전에 유사투자자문업 신고 요건을 확인할 것.


## 노션 학습 일지 기록

핵심 규칙 3번은 '출력'이 아니라 '기록'까지를 뜻한다. 채팅에 마크다운만 찍고
끝내지 말고 `src/report/notion_logger.py`의 `create_journal_entry()`로 실제
Notion 페이지를 만든다. 리스트를 넘기면 불릿으로 렌더링된다.

기능 단위 작업이 끝날 때마다 올린다. 에러 해결 내역에는 무엇을 고쳤는지뿐 아니라
**왜 그렇게 판단했는지**와 **틀린 가설이 있었다면 그것도** 남긴다.
